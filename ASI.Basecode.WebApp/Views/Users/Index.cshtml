@model ASI.Basecode.Services.ServiceModels.BookingViewModel
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
<link href="https://unpkg.com/flowbite@1.4.7/dist/flowbite.min.css" rel="stylesheet">

@{
    ViewData["Title"] = "User Home Page";
}
<link rel="stylesheet" href="~/css/admin.css" />
<link rel="stylesheet" href="~/css/admindark.css" />

<div class="outer-container">
    <div class="main-content">
        <div class="header">
            <div id="addBookingTab" class="tab active" onclick="switchTab('addBooking')">
                <i class="fas fa-plus"></i>
                <span>Add Booking</span>
            </div>
            <div id="myBookingsTab" class="tab" onclick="switchTab('myBookings')">
                <i class="fas fa-calendar-check"></i>
                <span>My Bookings</span>
            </div>
        </div>
        <div id="addBookingContent" class="content flex">
            <div class="left-pane">
                <div class="background-image">
                    <div class="overlay">
                        <div class="date-time">
                            <span id="time-now" class="time-now"></span><br>
                            <span id="date-now" class="date-now"></span>
                        </div>
                        <div class="book-now">
                            <button>Book now</button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="right-pane">
                <div class="booking-info">
                    <h1 class="text-3xl font-bold mb-4">Today's Booking</h1>
                    <div class="overflow-x-auto">
                        <div class="max-h-96 overflow-y-auto">
                            <table class="min-w-full bg-white border border-gray-200 rounded-lg">
                                <thead class="bg-gray-50">
                                    <tr>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Time</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Title</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Room</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody id="bookingItems" class="divide-y divide-gray-200">
                                      
                                </tbody>
                            </table>
                        </div>
                    </div>

                </div>
            </div>
        </div>
       <div id="myBookingsContent" class="content hidden">
            <div class="container mx-auto p-4 bg-gray-100" style="height: 79vh; display: flex; flex-direction: column; justify-content: space-between;">
                <h2 class="text-3xl font-bold mb-4 text-gray-800">My Bookings</h2>
                <div class="flex flex-col sm:flex-row justify-between items-center mb-6 space-y-4 sm:space-y-0">
                    <div class="search-container">
                        <input type="text" id="searchBar" class="w-full sm:w-2/3 p-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-blue-400 focus:border-blue-400" placeholder="Search...">
                        <button id="clearSearchButton" class="clear-button" style="display: none;">✖</button>
                    </div>
                    <div class="dropdown-container">
                        <button id="filterButton" class="w-full sm:w-auto sm:ml-4 px-4 py-2 bg-blue-600 text-white rounded-md flex items-center justify-center hover:bg-blue-700 transition duration-300 ease-in-out" onclick="toggleDropdown()">
                            <i class="fas fa-filter mr-2"></i>
                            Filter
                        </button>
                        <div id="roomDropdown" class="dropdown-content"></div>
                    </div>
                    <div class="book-now flex-grow">
                        <button class="w-20 px-4 py-2.5 text-sm text-white  rounded-md flex items-center justify-center hover:bg-red-700 transition duration-300 ease-in-out" style="background-color: #329a8f;font-size: large;margin-left: 0.5rem;left: auto;py: 3;" onclick="switchTab('addBooking')">
                            <i class="fas fa-plus mr-2"></i>
                            Add
                        </button>
                    </div>
                </div>
                <div class="bg-white shadow-lg rounded-lg flex-grow" style="overflow-y: auto;">
                    <div class="table-container" style="overflow-x: auto;">
                        <table id="bookingsTable" class="min-w-full max-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Booking Name</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Time</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Room</th>
                                    <th class="px-2 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Recurring</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                                </tr>
                            </thead>
                            <tbody id="bookingsTableBody" class="bg-white divide-y divide-gray-200">
                                <!-- Bookings will be displayed here -->
                            </tbody>
                        </table>
                    </div>
                </div>
                <div id="myBookingsPaginationContainer" class="flex justify-center items-center py-1" style="margin-top: 0.5rem;">
                    <div id="myBookingsPagination" class="flex justify-center items-center">
                        <!-- Pagination will be dynamically generated here -->
                    </div>
                </div>

            </div>
        </div>

    </div>
</div>


<div id="addMeetingModal" class="modal">
    <div class="modal-content">
        <span class="close">&times;</span>
        <h1 style="font-size: 2rem;">Booking Form</h1>
        <form id="bookingForm">
            <div class="form-group full-width">
                <label for="title">Meeting Title:</label>
                <input type="text" id="title" name="title" required>
            </div>

            <div class="form-group full-width">
                <label for="description">Meeting Description:</label>
                <input type="text" id="description" name="description">
            </div>

            <div class="form-group">
                <label for="date">Date:</label>
                <input type="date" id="date" name="date" value=@DateTime.Today.ToString("yyyy-MM-dd") required>
                <div id="dateError" class="error-message"></div>
            </div>

            <div class="form-group">
                <label for="room">Room:</label>
                <select id="room" name="room" required>
                    <option value="">Select Room</option>
                </select>
            </div>

            <div class="form-group">
                <label for="startTime">Start Time:</label>
                <select id="startTime" name="startTime" required>
                    <option value="00:00">12:00 AM</option>
                    <option value="00:15">12:15 AM</option>
                    <option value="00:30">12:30 AM</option>
                    <option value="00:45">12:45 AM</option>
                    <option value="01:00">01:00 AM</option>
                    <option value="01:15">01:15 AM</option>
                    <option value="01:30">01:30 AM</option>
                    <option value="01:45">01:45 AM</option>
                    <option value="02:00">02:00 AM</option>
                    <option value="02:15">02:15 AM</option>
                    <option value="02:30">02:30 AM</option>
                    <option value="02:45">02:45 AM</option>
                    <option value="03:00">03:00 AM</option>
                    <option value="03:15">03:15 AM</option>
                    <option value="03:30">03:30 AM</option>
                    <option value="03:45">03:45 AM</option>
                    <option value="04:00">04:00 AM</option>
                    <option value="04:15">04:15 AM</option>
                    <option value="04:30">04:30 AM</option>
                    <option value="04:45">04:45 AM</option>
                    <option value="05:00">05:00 AM</option>
                    <option value="05:15">05:15 AM</option>
                    <option value="05:30">05:30 AM</option>
                    <option value="05:45">05:45 AM</option>
                    <option value="06:00">06:00 AM</option>
                    <option value="06:15">06:15 AM</option>
                    <option value="06:30">06:30 AM</option>
                    <option value="06:45">06:45 AM</option>
                    <option value="07:00">07:00 AM</option>
                    <option value="07:15">07:15 AM</option>
                    <option value="07:30">07:30 AM</option>
                    <option value="07:45">07:45 AM</option>
                    <option value="08:00">08:00 AM</option>
                    <option value="08:15">08:15 AM</option>
                    <option value="08:30">08:30 AM</option>
                    <option value="08:45">08:45 AM</option>
                    <option value="09:00">09:00 AM</option>
                    <option value="09:15">09:15 AM</option>
                    <option value="09:30">09:30 AM</option>
                    <option value="09:45">09:45 AM</option>
                    <option value="10:00">10:00 AM</option>
                    <option value="10:15">10:15 AM</option>
                    <option value="10:30">10:30 AM</option>
                    <option value="10:45">10:45 AM</option>
                    <option value="11:00">11:00 AM</option>
                    <option value="11:15">11:15 AM</option>
                    <option value="11:30">11:30 AM</option>
                    <option value="11:45">11:45 AM</option>
                    <option value="12:00">12:00 PM</option>
                    <option value="12:15">12:15 PM</option>
                    <option value="12:30">12:30 PM</option>
                    <option value="12:45">12:45 PM</option>
                    <option value="13:00">01:00 PM</option>
                    <option value="13:15">01:15 PM</option>
                    <option value="13:30">01:30 PM</option>
                    <option value="13:45">01:45 PM</option>
                    <option value="14:00">02:00 PM</option>
                    <option value="14:15">02:15 PM</option>
                    <option value="14:30">02:30 PM</option>
                    <option value="14:45">02:45 PM</option>
                    <option value="15:00">03:00 PM</option>
                    <option value="15:15">03:15 PM</option>
                    <option value="15:30">03:30 PM</option>
                    <option value="15:45">03:45 PM</option>
                    <option value="16:00">04:00 PM</option>
                    <option value="16:15">04:15 PM</option>
                    <option value="16:30">04:30 PM</option>
                    <option value="16:45">04:45 PM</option>
                    <option value="17:00">05:00 PM</option>
                    <option value="17:15">05:15 PM</option>
                    <option value="17:30">05:30 PM</option>
                    <option value="17:45">05:45 PM</option>
                    <option value="18:00">06:00 PM</option>
                    <option value="18:15">06:15 PM</option>
                    <option value="18:30">06:30 PM</option>
                    <option value="18:45">06:45 PM</option>
                    <option value="19:00">07:00 PM</option>
                    <option value="19:15">07:15 PM</option>
                    <option value="19:30">07:30 PM</option>
                    <option value="19:45">07:45 PM</option>
                    <option value="20:00">08:00 PM</option>
                    <option value="20:15">08:15 PM</option>
                    <option value="20:30">08:30 PM</option>
                    <option value="20:45">08:45 PM</option>
                    <option value="21:00">09:00 PM</option>
                    <option value="21:15">09:15 PM</option>
                    <option value="21:30">09:30 PM</option>
                    <option value="21:45">09:45 PM</option>
                    <option value="22:00">10:00 PM</option>
                    <option value="22:15">10:15 PM</option>
                    <option value="22:30">10:30 PM</option>
                    <option value="22:45">10:45 PM</option>
                    <option value="23:00">11:00 PM</option>
                    <option value="23:15">11:15 PM</option>
                    <option value="23:30">11:30 PM</option>
                    <option value="23:45">11:45 PM</option>
                </select>
                <div id="startTimeError" class="error-message"></div>
                <span class="text-green-400 text-sm">UTC+8, Philippine Standard Time</span>
            </div>

            <div class="form-group">
                <label for="endTime">End Time:</label>
                <select id="endTime" name="endTime" required>
                    <option value="00:00">12:00 AM</option>
                    <option value="00:15">12:15 AM</option>
                    <option value="00:30">12:30 AM</option>
                    <option value="00:45">12:45 AM</option>
                    <option value="01:00">01:00 AM</option>
                    <option value="01:15">01:15 AM</option>
                    <option value="01:30">01:30 AM</option>
                    <option value="01:45">01:45 AM</option>
                    <option value="02:00">02:00 AM</option>
                    <option value="02:15">02:15 AM</option>
                    <option value="02:30">02:30 AM</option>
                    <option value="02:45">02:45 AM</option>
                    <option value="03:00">03:00 AM</option>
                    <option value="03:15">03:15 AM</option>
                    <option value="03:30">03:30 AM</option>
                    <option value="03:45">03:45 AM</option>
                    <option value="04:00">04:00 AM</option>
                    <option value="04:15">04:15 AM</option>
                    <option value="04:30">04:30 AM</option>
                    <option value="04:45">04:45 AM</option>
                    <option value="05:00">05:00 AM</option>
                    <option value="05:15">05:15 AM</option>
                    <option value="05:30">05:30 AM</option>
                    <option value="05:45">05:45 AM</option>
                    <option value="06:00">06:00 AM</option>
                    <option value="06:15">06:15 AM</option>
                    <option value="06:30">06:30 AM</option>
                    <option value="06:45">06:45 AM</option>
                    <option value="07:00">07:00 AM</option>
                    <option value="07:15">07:15 AM</option>
                    <option value="07:30">07:30 AM</option>
                    <option value="07:45">07:45 AM</option>
                    <option value="08:00">08:00 AM</option>
                    <option value="08:15">08:15 AM</option>
                    <option value="08:30">08:30 AM</option>
                    <option value="08:45">08:45 AM</option>
                    <option value="09:00">09:00 AM</option>
                    <option value="09:15">09:15 AM</option>
                    <option value="09:30">09:30 AM</option>
                    <option value="09:45">09:45 AM</option>
                    <option value="10:00">10:00 AM</option>
                    <option value="10:15">10:15 AM</option>
                    <option value="10:30">10:30 AM</option>
                    <option value="10:45">10:45 AM</option>
                    <option value="11:00">11:00 AM</option>
                    <option value="11:15">11:15 AM</option>
                    <option value="11:30">11:30 AM</option>
                    <option value="11:45">11:45 AM</option>
                    <option value="12:00">12:00 PM</option>
                    <option value="12:15">12:15 PM</option>
                    <option value="12:30">12:30 PM</option>
                    <option value="12:45">12:45 PM</option>
                    <option value="13:00">01:00 PM</option>
                    <option value="13:15">01:15 PM</option>
                    <option value="13:30">01:30 PM</option>
                    <option value="13:45">01:45 PM</option>
                    <option value="14:00">02:00 PM</option>
                    <option value="14:15">02:15 PM</option>
                    <option value="14:30">02:30 PM</option>
                    <option value="14:45">02:45 PM</option>
                    <option value="15:00">03:00 PM</option>
                    <option value="15:15">03:15 PM</option>
                    <option value="15:30">03:30 PM</option>
                    <option value="15:45">03:45 PM</option>
                    <option value="16:00">04:00 PM</option>
                    <option value="16:15">04:15 PM</option>
                    <option value="16:30">04:30 PM</option>
                    <option value="16:45">04:45 PM</option>
                    <option value="17:00">05:00 PM</option>
                    <option value="17:15">05:15 PM</option>
                    <option value="17:30">05:30 PM</option>
                    <option value="17:45">05:45 PM</option>
                    <option value="18:00">06:00 PM</option>
                    <option value="18:15">06:15 PM</option>
                    <option value="18:30">06:30 PM</option>
                    <option value="18:45">06:45 PM</option>
                    <option value="19:00">07:00 PM</option>
                    <option value="19:15">07:15 PM</option>
                    <option value="19:30">07:30 PM</option>
                    <option value="19:45">07:45 PM</option>
                    <option value="20:00">08:00 PM</option>
                    <option value="20:15">08:15 PM</option>
                    <option value="20:30">08:30 PM</option>
                    <option value="20:45">08:45 PM</option>
                    <option value="21:00">09:00 PM</option>
                    <option value="21:15">09:15 PM</option>
                    <option value="21:30">09:30 PM</option>
                    <option value="21:45">09:45 PM</option>
                    <option value="22:00">10:00 PM</option>
                    <option value="22:15">10:15 PM</option>
                    <option value="22:30">10:30 PM</option>
                    <option value="22:45">10:45 PM</option>
                    <option value="23:00">11:00 PM</option>
                    <option value="23:15">11:15 PM</option>
                    <option value="23:30">11:30 PM</option>
                    <option value="23:45">11:45 PM</option>
                </select>
                <div id="endTimeError" class="error-message"></div>
            </div>

            <div class="form-group full-width">
                <label for="recurring">Recurring:</label>
                <select id="recurring" name="recurring">
                    <option value="false">No</option>
                    <option value="true">Yes</option>
                </select>
            </div>

            <div id="recurrenceOptions" class="full-width" style="display: none;">
                <div class="form-group-half">
                    <label for="recurrenceType">Recurrence Type:</label>
                    <select id="RecurrenceTypeId" name="RecurrenceTypeId" class="form-control">
                        <option value="">Select Recurrence Type</option>
                    </select>
                </div>

                <div class="form-group-half">
                    <label for="recurrenceEndDate">Recurrence End Date:</label>
                    <input type="date" id="recurrenceEndDate" name="recurrenceEndDate">
                </div>
            </div>

            <div id="weeklyOptions" class="full-width" style="display: none;">
                <label>Select Days:</label>
                <div class="day-selector">
                    <div class="day" data-day="sunday">S</div>
                    <div class="day" data-day="monday">M</div>
                    <div class="day" data-day="tuesday">TU</div>
                    <div class="day" data-day="wednesday">W</div>
                    <div class="day" data-day="thursday">TH</div>
                    <div class="day" data-day="friday">F</div>
                    <div class="day" data-day="saturday">SA</div>
                </div>
            </div>

            <div class="form-group full-width">
                <div class="button-group">
                    <button type="reset" class="reset-button">Reset</button>
                    <button type="submit">Create Booking</button>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Modal for details -->
<div id="bookingDetailsModal" class="modal">
    <div class="modal-content">
        <span id="closeModalBtn" class="close">&times;</span>
        <h3>Booking Details</h3>
        <div id="bookingDetailsContent" class="white-container"></div>
    </div>
</div>

<!-- Modal for cancel -->
<div id="cancelModal" class="modal">
    <div class="modal-content">
        <span id="closeButt" class="close">&times;</span>
        <h1>Cancel Booking</h1>
        <p>Are you sure you want to cancel this booking?</p>
        <div class="button-container">
            <button id="confirmCancel" class="btn btn-danger">Yes, Cancel</button>
            <button id="cancelCancel" class="btn btn-secondary">No, Keep Booking</button>
        </div>
    </div>
</div>


<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@@emailjs/browser@3/dist/email.min.js"></script>
<script type="text/javascript">
     (function () {
         emailjs.init("GnqbHE6aetOc_pD7x");
     })();
</script>
<script>
    recurrenceTypes = ['Unassigned', 'Daily', 'Weekly', 'Monthly'];
    function showBookingDetails(bookingId) {
        fetch(`/api/Booking/${bookingId}`)
            .then(response => response.json())
            .then(booking => {
                fetch(`/api/Room/${booking.roomId}`)
                    .then(roomResponse => roomResponse.json())
                    .then(room => {
                        fetch('/api/Room/locations')
                            .then(locationResponse => locationResponse.json())
                            .then(locations => {
                                const location = locations.find(l => l.id === room.location);
                                const modal = document.getElementById('bookingDetailsModal');
                                const content = document.getElementById('bookingDetailsContent');
                                content.innerHTML = `
                        <p><strong>Meeting Title:</strong> ${booking.title}</p>
                        <p><strong>Meeting Description:</strong> ${booking.description}</p>
                        <p><strong>Date:</strong> ${new Date(booking.date).toLocaleDateString()}</p>
                        <p><strong>Time:</strong> ${booking.startTime} - ${booking.endTime}</p>
                        <p><strong>Room:</strong> ${booking.roomName}</p>
                        <p><strong>Location:</strong> ${location.name}</p>
                        <p><strong>Capacity:</strong> ${room.capacity}</p>
                        <p><strong>Facilities:</strong> ${room.facilities}</p>
                        <p><strong>Recurring:</strong> ${booking.recurring ? 'Yes' : 'No'}</p>
                        ${booking.recurring ? `
                            <p><strong>Recurrence Type:</strong> ${recurrenceTypes[booking.recurrenceTypeId]}</p>
                            <p><strong>Recurrence End Date:</strong> ${new Date(booking.recurrenceEndDate).toLocaleDateString()}</p>
                        ` : ''}
                    `;
                                modal.style.display = 'block';
                            })
                    })
            })
            .catch(error => console.error('Error fetching booking details:', error));
    }

    document.getElementById('closeModalBtn').onclick = function () {
        document.getElementById('bookingDetailsModal').style.display = 'none';
    }
        
        //this is for fetching today's bookings
       function fetchTodaysBookings() {
        fetch('/api/Booking/todaysBookings')
            .then(response => response.json())
            .then(data => {
                // Sort bookings by start time
                data.sort((a, b) => a.startTime.localeCompare(b.startTime));

                const bookingItemsContainer = document.getElementById('bookingItems');
                bookingItemsContainer.innerHTML = ''; // Clear existing items

                data.forEach(booking => {
                    const bookingItem = document.createElement('tr');
                    bookingItem.classList.add('booking-item');
                    bookingItem.innerHTML = `
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">${formatTime(booking.startTime)} - ${formatTime(booking.endTime)}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">${booking.title}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">${booking.roomName}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                            <button class="details-btn text-green-600 hover:text-green-900" data-id="${booking.id}">
                                <i class="fas fa-info-circle"></i>Details
                            </button>
                        </td>
                    `;
                    bookingItemsContainer.appendChild(bookingItem);
                });

                // Add event listeners to the new buttons
                document.querySelectorAll('#bookingItems .details-btn').forEach(btn => {
                    btn.addEventListener('click', function () {
                        showBookingDetails(this.dataset.id);
                    });
                });
            })
            .catch(error => console.error('Error fetching bookings:', error));
    }


    function formatTime(timeString) {
        const [hours, minutes] = timeString.split(':');
        let hoursInt = parseInt(hours);
        const ampm = hoursInt >= 12 ? 'PM' : 'AM';
        hoursInt = hoursInt % 12 || 12; // Convert to 12-hour format
        return `${hoursInt}:${minutes} ${ampm}`;
    }

    function updateDateTime() {
        const now = new Date();

        // pag update sa time
        const timeElement = document.getElementById('time-now');
        const currentTime = now.toTimeString().slice(0, 5); // HH:MM
        timeElement.textContent = formatTime(currentTime);

        // pag update sa day
        const dateElement = document.getElementById('date-now');
        dateElement.textContent = now.toLocaleString('en-US', {
            weekday: 'long',
            month: 'long',
            day: 'numeric'
        });
    }

    //real time update sa time and date 
    updateDateTime();
    setInterval(updateDateTime, 1000);

    rooms = [];

    document.addEventListener("DOMContentLoaded", function () {
        // Fetch rooms and populate the select element
        fetch('/api/Booking/rooms')
            .then(response => response.json())
            .then(data => {
                const roomSelect = document.getElementById('room');
                const roomDropdown = $('#roomDropdown');
                roomDropdown.empty();

                data.forEach(room => {
                    const option = document.createElement('option');
                    option.value = room.id;
                    option.text = room.name;
                    roomSelect.appendChild(option);
                    rooms.push(room.name);
                    roomDropdown.append(
                        $('<a></a>')
                        .attr('onclick', `filterByRoom('${room.name}')`)
                        .text(room.name)
                    );
                });
                console.log(rooms);
                roomDropdown.append($('<a></a>').attr('onclick', 'showAllRooms()').text('Show All'));
            })
            .catch(error => console.error('Error fetching rooms:', error));

        //For fetching recurr types
            function populateRecurrenceTypes() {
                $.ajax({
                    url: '/api/Booking/recurrenceTypes',
                    type: 'GET',
                    success: function (data) {
                        var dropdown = $('#RecurrenceTypeId');
                        dropdown.empty();
                        dropdown.append($('<option></option>').val('').text('Select Recurrence Type'));
                        $.each(data, function (i, item) {
                            dropdown.append($('<option></option>').val(item.id).text(item.name));
                        });
                    },
                    error: function (error) {
                        console.log('Error fetching recurrence types:', error);
                    }
                });
            }

        // Call this function when the page loads
        $(document).ready(function () {
            populateRecurrenceTypes();
        });

        // Add form validation
        function validateForm() {
            const requiredFields = bookingForm.querySelectorAll('[required]');
            let isValid = true;
            requiredFields.forEach(field => {
                if (!field.value.trim()) {
                    isValid = false;
                    field.classList.add('invalid');
                } else {
                    field.classList.remove('invalid');
                }
            });
            return isValid;
        }

        // format time to TimeSpan string
        function formatTimeToTimeSpan(timeString) {
            const [hours, minutes] = timeString.split(':');
            return `${hours.padStart(2, '0')}:${minutes}:00`;
        }

        // Converts DayOfPeriod number to text format
        function stringifyDayOfPeriod(dayOfPeriod) {
            switch(dayOfPeriod) {
                case 0:
                    return "Unassigned";
                case 62:
                    return "Weekdays";
                case 65:
                    return "Weekends";
                case 127:
                    return "Everyday";
                default:
                    let week = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
                    let day_list = [];
                    temp = dayOfPeriod;
                    for(let i = 0; i < 7; i++) {
                        day_list.push(~~(temp / 2 ** [6 - i]));
                        temp %= 2 ** [6 - i];
                    }
                    let count = day_list.reduce((sum, num) => sum + num, 0);
                    if(count == 6) {
                        let i = 0;
                        let list = [];
                        day_list.forEach(day => {
                            list[i] = day == 0   ? week[i] : "";
                            i++;
                        });
                        return "Except " + list.join("");
                    } else {
                        switch(count) {
                            case 1:
                                let i = 0;
                                let list = [];
                                day_list.forEach(day => {
                                    list[i] = day == 1 ? week[i] : "";
                                    i++;
                                });
                                return "Every " + list.join("");
                            case 2:
                                return "Twice A Week";
                            case 3:
                                return "3 Days A Week";
                            case 4:
                                return "4 Days A Week";
                            case 5:
                                return "5 Days A Week";
                            default:
                                return "Error";
                        }
                    }
                    return "Error";
            }
        }

        const bookingForm = document.getElementById('bookingForm');
        const submitButton = bookingForm.querySelector('button[type="submit"]');

        let currentPage = 1;
        const itemsPerPage = 5;
        let bookingsData = [];

        function loadBookings(page = 1, searchQuery='') {
            fetch('/api/Booking/myBookings')
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP error! status: ${response.status}`);
                    }
                    return response.json();
                })
                .then(data => {
                    bookingsData = data;
                    if (searchQuery) {
                        bookingsData = data.filter(booking => booking.title.toLowerCase().includes(searchQuery.toLowerCase()));
                    }

                    const totalItems = bookingsData.length;
                    const totalPages = Math.ceil(totalItems / itemsPerPage);
                    currentPage = page;

                    const startIndex = (currentPage - 1) * itemsPerPage;
                    const endIndex = startIndex + itemsPerPage;
                    const currentItems = bookingsData.slice(startIndex, endIndex);

                    renderTable(currentItems);
                    generatePagination(totalPages);
                })
                .catch(error => console.error('Error fetching bookings:', error));
        }

        function renderTable(data) {
            const tableBody = document.getElementById('bookingsTableBody');
            tableBody.innerHTML = '';

            data.forEach(booking => {
                const row = document.createElement('tr');
                row.innerHTML = `
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">${booking.title}</td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">${new Date(booking.date).toLocaleDateString() + (booking.recurring ? " - " + new Date(booking.recurrenceEndDate).toLocaleDateString() : "")}</td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">${booking.startTime} - ${booking.endTime}</td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">${booking.roomName}</td>
                <td class="px-3 py-4 whitespace-nowrap text-sm text-gray-500">
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full ${booking.recurring ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}">
                        ${booking.recurring ? booking.recurrenceTypeId == 2 ? stringifyDayOfPeriod(booking.recurrenceDayOfPeriod) : recurrenceTypes[booking.recurrenceTypeId] : 'No'}
                    </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                    <button class="details-btn text-green-600 hover:text-green-900 mr-2" data-id="${booking.id}">Details</button>
                    <button class="edit-btn text-blue-600 hover:text-blue-900 mr-2" data-id="${booking.id}">Edit</button>
                    <button class="cancel-btn text-red-600 hover:text-red-900" data-id="${booking.id}">Cancel</button>
                </td>
            `;
                tableBody.appendChild(row);
            });

            addEventListeners();
        }

        function generatePagination(totalPages) {
            const pagination = document.getElementById('myBookingsPagination');
            pagination.innerHTML = '';

            const prevButton = document.createElement('button');
            prevButton.innerHTML = '← Previous';
            prevButton.className = `px-3 py-1 mx-1 rounded text-sm ${currentPage === 1 ? 'bg-gray-200 text-gray-400 cursor-not-allowed' : 'bg-gray-600 text-white hover:bg-gray-900'}`;
            prevButton.disabled = currentPage === 1;
            prevButton.onclick = () => loadBookings(currentPage - 1);
            pagination.appendChild(prevButton);

            for (let i = 1; i <= totalPages; i++) {
                const pageButton = document.createElement('button');
                pageButton.innerHTML = i;
                pageButton.className = `px-3 py-1 mx-1 rounded text-sm ${i === currentPage ? 'bg-green-700 text-white' : 'bg-gray-200 text-gray-700 hover:bg-gray-300'}`;
                pageButton.onclick = () => loadBookings(i);
                pagination.appendChild(pageButton);
            }

            const nextButton = document.createElement('button');
            nextButton.innerHTML = 'Next →';
            nextButton.className = `px-3 py-1 mx-1 rounded text-sm ${currentPage === totalPages ? 'bg-gray-200 text-gray-400 cursor-not-allowed' : 'bg-gray-600 text-white hover:bg-gray-900'}`;
            nextButton.disabled = currentPage === totalPages;
            nextButton.onclick = () => loadBookings(currentPage + 1);
            pagination.appendChild(nextButton);
        }

        // Event listener for search button
        document.getElementById('searchBar').addEventListener('keydown', (event) => {
            if (event.key === 'Enter') {
                const searchQuery = event.target.value;
                loadBookings(1, searchQuery); // Load the first page with the search query
                event.preventDefault(); // Prevent form submission or other default behavior
            }
        });

        // Event listener for clear search button
        const searchInput = document.getElementById('searchBar');
        const clearButton = document.getElementById('clearSearchButton');

        searchInput.addEventListener('input', () => {
            if (searchInput.value) {
                clearButton.style.display = 'block'; // Show the clear button
            } else {
                clearButton.style.display = 'none'; // Hide the clear button
            }
        });

        clearButton.addEventListener('click', () => {
            searchInput.value = ''; // Clear the search input
            clearButton.style.display = 'none'; // Hide the clear button
            loadBookings(1); // Reload rooms without search query
        });

        function addEventListeners() {
            document.querySelectorAll('.edit-btn').forEach(btn => {
                btn.addEventListener('click', () => editBooking(btn.dataset.id));
            });
            document.querySelectorAll('.details-btn').forEach(btn => {
                btn.addEventListener('click', () => showBookingDetails(btn.dataset.id));
            });
            document.querySelectorAll('.cancel-btn').forEach(btn => {
                btn.addEventListener('click', () => cancelBooking(btn.dataset.id));
            });
        }
        // i laod ang bookings
        fetchTodaysBookings();
        loadBookings();

        // Form submission
        bookingForm.addEventListener('submit', function (e) {
            e.preventDefault();

            if (!validateForm()) {
                alert('Please fill in all required fields.');
                return;
            }

            const formData = new FormData(this);
            const startTime = formData.get('startTime');
            const endTime = formData.get('endTime');
            const date = formData.get('date');

            // Validate date is not before the current date
            const currentDate = new Date();
            const selectedDate = new Date(date);
            currentDate.setHours(0, 0, 0, 0);
            selectedDate.setHours(0, 0, 0, 0);

            if (selectedDate < currentDate) {
                document.getElementById('dateError').textContent = 'Selected date cannot be in the past.';
                document.getElementById('dateError').style.display = 'block';
                return;
            }

            // Validate start time is not before the current time if booking is for today
            const currentDateTime = new Date();
            const selectedDateTime = new Date(`${date}T${startTime}`);
            if (selectedDate.getTime() === currentDate.getTime() && selectedDateTime < currentDateTime) {
                document.getElementById('startTimeError').textContent = 'Start time cannot be in the past.';
                document.getElementById('startTimeError').style.display = 'block';
                return;
            }

            // Validate end time is at least 15 minutes after start time
            const startTimeDate = new Date(`2000-01-01T${startTime}`);
            const endTimeDate = new Date(`2000-01-01T${endTime}`);
            const timeDifference = (endTimeDate - startTimeDate) / (1000 * 60); // difference in minutes
            if (timeDifference < 15) {
                document.getElementById('endTimeError').textContent = 'End time must be at least 15 minutes after start time.';
                document.getElementById('endTimeError').style.display = 'block';
                return;
            }

            // Get day of period for recurrence
            let dayOfPeriod = 0;
            document.querySelectorAll('.day-selector .day').forEach(function (day) {
                let data = day.getAttribute('data-day');
                if(day.classList.contains('selected')) {
                    switch (data) {
                        case 'sunday':
                            dayOfPeriod += 64;
                            break;
                        case 'monday':
                            dayOfPeriod += 32;
                            break;
                        case 'tuesday':
                            dayOfPeriod += 16;
                            break;
                        case 'wednesday':
                            dayOfPeriod += 8;
                            break;
                        case 'thursday':
                            dayOfPeriod += 4;
                            break;
                        case 'friday':
                            dayOfPeriod += 2;
                            break;
                        case 'saturday':
                            dayOfPeriod += 1;
                            break;
                    }
                }
            });

            const data = {
                title: formData.get('title'),
                description: formData.get('description'),
                date: formData.get('date'),
                roomId: parseInt(formData.get('room')),
                startTime: formatTimeToTimeSpan(startTime),
                endTime: formatTimeToTimeSpan(endTime),
                recurring: formData.get('recurring') === 'true',
                userId: getCurrentUserId()
            };

            if (data.recurring) {
                data.recurrenceTypeId = parseInt(formData.get('RecurrenceTypeId')) || null;
                data.recurrenceEndDate = formData.get('recurrenceEndDate');
                data.recurrenceDayOfPeriod = dayOfPeriod;
            }

            if (this.dataset.mode === 'edit') {
                data.id = parseInt(this.dataset.bookingId);
            }

            if (data.recurring && !data.recurrenceTypeId) {
                document.getElementById('recurrenceTypeError').textContent = 'Please select a recurrence type.';
                document.getElementById('recurrenceTypeError').style.display = 'block';
                submitButton.disabled = false;
                submitButton.textContent = this.dataset.mode === 'edit' ? 'Update Booking' : 'Create Booking';
                return;
            }

            const isEdit = this.dataset.mode === 'edit';
            const url = isEdit ? `/api/Booking/${this.dataset.bookingId}` : '/api/Booking/create';
            const method = isEdit ? 'PUT' : 'POST';

            fetch(url, {
                method: method,
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(data)
            })
                .then(response => {
                    if (!response.ok) {
                        return response.json().then(err => { throw err; });
                    }
                    return response.json();
                })
                .then(result => {
                    console.log('Booking ' + (isEdit ? 'updated' : 'created') + ':', result);
                    modal.style.display = "none";
                    loadBookings();
                    fetchTodaysBookings();
                    resetFormAndButton();

                    this.dataset.mode = 'create';
                    delete this.dataset.bookingId;
                    submitButton.textContent = 'Create Booking';

                    const today = new Date().toISOString().split('T')[0];
                    if (data.date === today) {
                        fetchTodaysBookings();
                    }

                    if (isEdit) {
                        toastr.success('Booking updated successfully!');
                    } else {
                        toastr.success('Booking created successfully!');
                    }

                    // Send confirmation email if booking was created (not edited)
                    if (!isEdit) {
                        // Fetch user details
                        fetch(`/api/UserManagement/${data.userId}`)
                            .then(response => {
                                if (!response.ok) {
                                    throw new Error('User not found');
                                }
                                return response.json();
                            })
                            .then(user => {
                                // Fetch room details
                                fetch(`/api/Room/${data.roomId}`)
                                    .then(response => {
                                        if (!response.ok) {
                                            throw new Error('Room not found');
                                        }
                                        return response.json();
                                    })
                                    .then(room => {
                                        sendBookingConfirmationEmail(data, user.email, user.firstName, room.name);
                                    });
                            })
                            .catch(error => {
                                console.error('Error fetching user or room details:', error);
                            });
                    }
                })
                .catch(error => {
                    console.error('Error ' + (isEdit ? 'updating' : 'creating') + ' booking:', error);
                    let errorMessage = 'An error occurred while ' + (isEdit ? 'updating' : 'creating') + ' the booking. Please try again.';
                    if (error.errors) {
                        errorMessage += '\n\nDetails:\n' + JSON.stringify(error.errors, null, 2);
                    }
                    alert(errorMessage);
                })
                .finally(() => {
                    submitButton.disabled = false;
                    submitButton.textContent = isEdit ? 'Update Booking' : 'Create Booking';
                });
        });

        function sendBookingConfirmationEmail(bookingData, userEmail, userName, roomName) {
            const templateParams = {
                to_name: userName,
                room_name: roomName,
                booking_date: bookingData.date,
                start_time: bookingData.startTime,
                end_time: bookingData.endTime
            };

            emailjs.send('service_ok8rjwn', 'template_hite8j1', templateParams)
                .then((response) => {
                    console.log('Email sent successfully:', response);
                }, (error) => {
                    console.error('Failed to send email:', error);
                });
        }


        function convertTimeToMinutes(time) {
            const [hours, minutes] = time.split(':').map(Number);
            return hours * 60 + minutes;
        }

        //15 minute interval for auto fill end time
        function getNearestFuture15MinInterval(date) {
            const minutes = date.getMinutes();
            const roundedMinutes = Math.ceil(minutes / 15) * 15;
            const newDate = new Date(date);
            newDate.setMinutes(roundedMinutes);
            newDate.setSeconds(0);
            newDate.setMilliseconds(0);
            return newDate;
        }

        //set initial values for start and end time
        document.getElementById('startTime').addEventListener('change', function () {
            const startTime = new Date(`2000-01-01T${this.value}`);
            const endTime = new Date(`2000-01-01T${document.getElementById('endTime').value}`);

            if (endTime <= startTime) {
                const newEndTime = new Date(startTime.getTime() + 15 * 60000);
                document.getElementById('endTime').value = newEndTime.toTimeString().substring(0, 5);
            }
        });

        //start time and end time validation
        document.getElementById('startTime').addEventListener('change', validateTimes);
        document.getElementById('endTime').addEventListener('change', validateTimes);
        document.getElementById('date').addEventListener('change', validateTimes);

        function validateTimes() {
            validateStartTime();
            validateEndTime();
        }

        function validateStartTime() {
            const startTimeSelect = document.getElementById('startTime');
            const dateInput = document.getElementById('date');
            const errorElement = document.getElementById('startTimeError');

            const selectedDate = new Date(dateInput.value);
            const selectedTime = startTimeSelect.value;
            const selectedDateTime = new Date(`${selectedDate.toDateString()} ${selectedTime}`);
            const currentDateTime = new Date();

            if (selectedDateTime < currentDateTime) {
                errorElement.textContent = 'Start time cannot be in the past.';
                errorElement.style.display = 'block';
                startTimeSelect.setCustomValidity('Invalid start time');
            } else {
                errorElement.textContent = '';
                errorElement.style.display = 'none';
                startTimeSelect.setCustomValidity('');
            }
        }

        function validateEndTime() {
            const startTimeSelect = document.getElementById('startTime');
            const endTimeSelect = document.getElementById('endTime');
            const errorElement = document.getElementById('endTimeError');

            const startTime = new Date(`2000-01-01T${startTimeSelect.value}`);
            const endTime = new Date(`2000-01-01T${endTimeSelect.value}`);

            const timeDifference = (endTime - startTime) / (1000 * 60); // difference in minutes

            if (timeDifference < 15) {
                errorElement.textContent = 'End time must be at least 15 minutes after start time.';
                errorElement.style.display = 'block';
                endTimeSelect.setCustomValidity('Invalid end time');
            } else {
                errorElement.textContent = '';
                errorElement.style.display = 'none';
                endTimeSelect.setCustomValidity('');
            }
        }

        //date validation
        document.getElementById('date').addEventListener('change', validateDate);

        function validateDate() {
            const dateInput = document.getElementById('date');
            const errorElement = document.getElementById('dateError');
            const selectedDate = new Date(dateInput.value);
            const currentDate = new Date();

            // Set both dates to midnight for accurate comparison
            selectedDate.setHours(0, 0, 0, 0);
            currentDate.setHours(0, 0, 0, 0);

            if (selectedDate < currentDate) {
                errorElement.textContent = 'Selected date cannot be in the past.';
                errorElement.style.display = 'block';
                dateInput.setCustomValidity('Invalid date');
            } else {
                errorElement.textContent = '';
                errorElement.style.display = 'none';
                dateInput.setCustomValidity('');
            }
        }

        function editBooking(bookingId) {
            fetch(`/api/Booking/${bookingId}`)
                .then(response => response.json())
                .then(booking => {
                    // Populate the form with booking details
                    document.getElementById('title').value = booking.title;
                    document.getElementById('description').value = booking.description;
                    document.getElementById('date').value = booking.date.split('T')[0];
                    document.getElementById('room').value = booking.roomId;
                    document.getElementById('startTime').value = booking.startTime;
                    document.getElementById('endTime').value = booking.endTime;
                    document.getElementById('recurring').value = booking.recurring ? 'true' : 'false';

                    if (booking.recurring) {
                        document.getElementById('recurrenceOptions').style.display = 'block';
                        document.getElementById('RecurrenceTypeId').value = booking.recurrenceTypeId;
                        document.getElementById('recurrenceEndDate').value = booking.recurrenceEndDate.split('T')[0];
                    }

                    // Populate day of week
                    console.log("RID: " + booking.recurrenceTypeId);
                    if(booking.recurrenceTypeId == 2) {
                        document.getElementById('weeklyOptions').style.display = 'block';
                        let day_list = [];
                        let temp = booking.recurrenceDayOfPeriod;
                        for (let i = 0; i < 7; i++) {
                            day_list.push(~~(temp / 2 ** [6 - i]));
                            temp %= 2 ** [6 - i];
                        }
                        document.querySelectorAll('.day-selector .day').forEach(function (day) {
                            let data = day.getAttribute('data-day');
                            switch (data) {
                                case 'sunday':
                                    (day_list[0] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                                case 'monday':
                                    (day_list[1] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                                case 'tuesday':
                                    (day_list[2] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                                case 'wednesday':
                                    (day_list[3] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                                case 'thursday':
                                    (day_list[4] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                                case 'friday':
                                    (day_list[5] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                                case 'saturday':
                                    (day_list[6] == 1) ? day.classList.add('selected') : day.classList.remove('selected');
                                    break;
                            }
                        });
                    }
                    
                    // Set initial values for start and end time
                    const startTime = new Date(`2000-01-01T${booking.startTime}`);
                    const endTime = new Date(startTime.getTime() + 15 * 60000);
                    document.getElementById('endTime').value = endTime.toTimeString().substring(0, 5);

                    // Change form submission to update instead of create
                    bookingForm.dataset.mode = 'edit';
                    bookingForm.dataset.bookingId = bookingId;

                    // Change submit button text to 'Update Booking'
                    submitButton.textContent = 'Update Booking';

                    // Show the modal
                    modal.style.display = "block";
                })
                .catch(error => console.error('Error fetching booking details:', error));
        }

       

        function closeBookingDetailsModal() {
            const modal = document.getElementById('bookingDetailsModal');
            modal.classList.add('hidden');
        }

        function closeCancelModal() {
            const modal = document.getElementById('cancelModal');
            modal.classList.add('hidden');
        }

        // Event listener for the close button
        document.getElementById('closeModalBtn').addEventListener('click', closeBookingDetailsModal);
        document.getElementById('closeModalBtn').addEventListener('click', closeCancelModal);

        // Event listener for closing the modal when clicking outside the content
        document.getElementById('bookingDetailsModal').addEventListener('click', function (event) {
            if (event.target === this) {
                closeBookingDetailsModal();
            }
        });

        //function for cancel
        function cancelBooking(bookingId) {
            const modal = document.getElementById('cancelModal');
            const confirmBtn = document.getElementById('confirmCancel');
            const cancelBtn = document.getElementById('cancelCancel');
            const exitBtn = document.getElementById('closeButt');

            modal.style.display = 'block';

            confirmBtn.onclick = function () {
                modal.style.display = 'none';
                proceedWithCancellation(bookingId);
            }

            exitBtn.onclick = function () {
                modal.style.display = 'none';
            }

            cancelBtn.onclick = function () {
                modal.style.display = 'none';
            }

            window.onclick = function (event) {
                if (event.target == modal) {
                    modal.style.display = 'none';
                }
            }
        }

        function proceedWithCancellation(bookingId) {
            fetch(`/api/Booking/${bookingId}`, { method: 'DELETE' })
                .then(response => {
                    if (response.ok) {
                        return response.json();
                    }
                    return response.json().then(err => Promise.reject(err));
                })
                .then(data => {
                    // Remove the booking from tables
                    removeBookingFromTables(bookingId);

                    toastr.success('Booking cancelled successfully');

                    // Fetch booking details
                    return fetch(`/api/Booking/${bookingId}`);
                })
                .then(response => {
                    if (!response.ok) {
                        throw new Error('Booking not found');
                    }
                    return response.json();
                })
                .then(booking => {
                    // Fetch user details
                    return Promise.all([
                        fetch(`/api/UserManagement/${booking.userId}`),
                        fetch(`/api/Room/${booking.roomId}`),
                        Promise.resolve(booking)
                    ]);
                })
                .then(([userResponse, roomResponse, booking]) => {
                    if (!userResponse.ok || !roomResponse.ok) {
                        throw new Error('User or Room not found');
                    }
                    return Promise.all([userResponse.json(), roomResponse.json(), Promise.resolve(booking)]);
                })
                .then(([user, room, booking]) => {
                    sendCancelNotificationEmail(booking, user.email, user.firstName, room.name);
                })
                .catch(error => {
                    console.error('Error cancelling booking:', error);
                    toastr.error(error.message || 'Failed to cancel booking. Please try again.');
                });
        }

        function sendCancelNotificationEmail(bookingData, userEmail, userName, roomName) {
            const templateParams = {
                to_name: userName,
                room_name: roomName,
                booking_date: bookingData.date,
                start_time: bookingData.startTime,
                end_time: bookingData.endTime
            };

            emailjs.send('service_ok8rjwn', 'template_wn0xrna', templateParams)
                .then((response) => {
                    console.log('Cancellation email sent successfully:', response);
                }, (error) => {
                    console.error('Failed to send cancellation email:', error);
                });
        }

        function removeBookingFromTables(bookingId) {
            // Remove from My Bookings table
            const myBookingsButton = document.querySelector(`#bookingsTableBody button[data-id="${bookingId}"]`);
            if (myBookingsButton) {
                const myBookingsRow = myBookingsButton.closest('tr');
                if (myBookingsRow) {
                    myBookingsRow.remove();
                }
            }

            // Remove from Today's Bookings table
            const todaysBookingsButton = document.querySelector(`#bookingItems button[data-id="${bookingId}"]`);
            if (todaysBookingsButton) {
                const todaysBookingsRow = todaysBookingsButton.closest('tr');
                if (todaysBookingsRow) {
                    todaysBookingsRow.remove();
                }
            }

            // Update display if Today's Bookings is empty
            const bookingItems = document.getElementById('bookingItems');
            if (bookingItems && bookingItems.children.length === 0) {
                const todaysBookingsSection = document.getElementById('todaysBookingsSection');
                const noBookingsMessage = document.getElementById('noBookingsMessage');
                if (todaysBookingsSection) todaysBookingsSection.style.display = 'none';
                if (noBookingsMessage) noBookingsMessage.style.display = 'block';
            }
        }

        // reset the form and button
        function resetFormAndButton() {
            bookingForm.reset();
            bookingForm.dataset.mode = 'create';
            delete bookingForm.dataset.bookingId;
            submitButton.textContent = 'Create Booking';
            document.getElementById('recurrenceOptions').style.display = 'none';
        }

        // pag open/close sa modal
        var modal = document.getElementById("addMeetingModal");
        var btn = document.querySelector(".book-now button");
        var span = document.getElementsByClassName("close")[0];

        btn.onclick = function () {
            modal.style.display = "block";
            resetFormAndButton(); // Reset form when opening for a new booking

            // Get the nearest future 15-minute interval
            const now = new Date();
            const startTime = getNearestFuture15MinInterval(now);

            // Set end time to 15 minutes after start time
            const endTime = new Date(startTime.getTime() + 15 * 60000);

            // Format times for the select inputs
            const formatTimeForSelect = (date) => {
                return date.toTimeString().substring(0, 5);
            };

            // Set the start and end time in the form
            document.getElementById('startTime').value = formatTimeForSelect(startTime);
            document.getElementById('endTime').value = formatTimeForSelect(endTime);
        }

        span.onclick = function () {
            modal.style.display = "none";
            resetFormAndButton(); // Reset form when closing modal
        }

        window.onclick = function (event) {
            if (event.target == modal) {
                modal.style.display = "none";
                resetFormAndButton(); // Reset form when closing modal by clicking outside
            }
        }

        // Show/hide recurrence options
        document.getElementById('recurring').addEventListener('change', function () {
            const isRecurring = this.value === 'true';
            document.getElementById('recurrenceOptions').style.display = isRecurring ? 'flex' : 'none';
            if (!isRecurring) {
                document.getElementById('RecurrenceTypeId').value = '';
                document.getElementById('recurrenceEndDate').value = '';
                document.getElementById('weeklyOptions').style.display = 'none';
                //document.getElementById('day-selector').value = '';
            }
        });

        // Show/hide weekly and monthly options
        document.getElementById('RecurrenceTypeId').addEventListener('change', function () {
            const selectedType = this.options[this.selectedIndex].text;
            document.getElementById('weeklyOptions').style.display = selectedType === 'Weekly' ? 'block' : 'none';
        });

        // Day selection functionality
        document.querySelectorAll('.day-selector .day').forEach(function (day) {
            day.addEventListener('click', function () {
                this.classList.toggle('selected');
            });
        });
        
        // reset function
        document.querySelector('.reset-button').addEventListener('click', function () {
            document.getElementById('recurrenceOptions').style.display = 'none';
            document.getElementById('RecurrenceTypeId').value = '';
            document.getElementById('recurrenceEndDate').value = '';
            document.getElementById('weeklyOptions').style.display = 'none';
            document.querySelectorAll('.day-selector .day').forEach(function (day) {
                day.classList.remove('selected');
            });
        });

        // get current user ID (to be implemented)
        function getCurrentUserId() {
            // should return the ID of the currently logged-in user
            // pwede from global var, local stor, or API call
            return parseInt(@HttpContextAccessor.HttpContext.Session.GetString("UserId"));
        }
    }); 

    function searchFilterBookings() {
        var input, filter, table, tr, td, i, j, txtValue;
        input = document.getElementById("searchBar");
        filter = input.value.toUpperCase();
        table = document.getElementById("bookingsTable");
        tr = table.getElementsByTagName("tr");

        for (i = 1; i < tr.length; i++) { // Start from 1 to skip the header row
            let rowContainsFilter = false;
            for (j = 0; j < 3; j++) { // Check the first three columns
                td = tr[i].getElementsByTagName("td")[j];
                if (td) {
                    txtValue = (td.textContent || td.innerText).toUpperCase();
                    if (txtValue.indexOf(filter) > -1) {
                        rowContainsFilter = true;
                        break;
                    }
                }
            }
            if (rowContainsFilter) {
                tr[i].style.display = "";
            } else {
                tr[i].style.display = "none";
            }
        }
    }

    function toggleDropdown() {
        document.getElementById('roomDropdown').classList.toggle('show');
    }

    function filterByRoom(room) {
        const filter = room;
        const table = document.getElementById('bookingsTable');
        const tr = table.getElementsByTagName('tr');
        for (let i = 1; i < tr.length; i++) {
            const td = tr[i].getElementsByTagName('td')[3];
            if (td) {
                const txtValue = (td.textContent || td.innerText).trim();
                console.log(filter);
                if (txtValue === filter) {
                    tr[i].style.display = '';
                } else {
                    tr[i].style.display = 'none';
                }
            }
        }
        document.getElementById('roomDropdown').classList.remove('show');
    }

    function showAllRooms() {
        const table = document.getElementById('bookingsTable');
        const tr = table.getElementsByTagName('tr'); 
        for (let i = 1; i < tr.length; i++) {
            tr[i].style.display = '';
        }
        document.getElementById('roomDropdown').classList.remove('show');
    }
</script>


@*Switch Tabs Script*@
<script>
    function switchTab(tabName) {
        // Taguan ani tanan content
        document.getElementById('addBookingContent').style.display = 'none';
        document.getElementById('myBookingsContent').style.display = 'none';

        // Kuhaon ang active class sa tanan nga tabs
        document.getElementById('addBookingTab').classList.remove('active');
        document.getElementById('myBookingsTab').classList.remove('active');

        // Ipakita ang gipili nga content ug ibutang ang active class
        document.getElementById(tabName + 'Content').style.display = 'flex';
        document.getElementById(tabName + 'Tab').classList.add('active');
    }
</script>

@section Styles {
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            width: 100%;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            background-color: #000;
        }

        .outer-container {
            box-sizing: border-box;
            background-color: var(--light);
            padding: 10px;
            border-radius: 15px;
            display: flex;
            flex-direction: column-reverse;
            flex-direction: column;
            width: calc(100% - 265px);
            height: calc(100%-40px);
            top: 20px;
            right: 20px;
            bottom: 20px;
            margin-left: 100px;
            position: fixed;
            z-index: 0;
        }

        .notification-profile {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            position: absolute;
            top: 20px;
            right: 20px;
        }

        .notification {
            position: relative;
            margin-right: 20px;
            color: #185D62;
            font-size: 24px;
            cursor: pointer;
        }

        .badge {
            position: absolute;
            top: -10px;
            right: -10px;
            background-color: red;
            color: white;
            font-size: 12px;
            padding: 4px 6px;
            border-radius: 50%;
        }

        .profile-picture {
            position: relative;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            overflow: hidden;
            border: 2px solid #1BC7D2;
        }

            .profile-picture img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                border-radius: 50%;
            }

        .main-content {
            height: 85vh;
            background-color: #185D62;
            padding: 20px;
            color: #fff;
            border-radius: 10px;
            width: 100%;
            box-sizing: border-box;
            flex-grow: 1;
        }

        .header {
            display: flex;
            justify-content: flex-start;
            margin-bottom: 20px;
            border-bottom: 2px solid #1BC7D2;
        }

            .header .tab {
                padding: 10px 20px;
                cursor: pointer;
                background-color: #185D62;
                color: #fff;
                border-radius: 5px 5px 0 0;
                margin-right: 10px;
            }

                .header .tab.active {
                    background-color: #1BC7D2;
                    color: #000;
                }

        .content {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            height: 100%;
            overflow-y: hidden;
        }

        .left-pane, .right-pane {
            flex: 1;
            margin: 10px;
        }

        .background-image {
            background-image: url("/img/book.jpg");
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            width: 100%;
            height: 85%;
            position: relative;
            border-radius: 10px;
            overflow: hidden;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: #fff;
            text-align: center;
        }

        .date-time {
            margin-bottom: 100px;
        }

            .date-time .time-now,
            .date-time .date-now {
                font-size: 40px;
                line-height: 1.2;
                margin-bottom: 10px;
            }

        .book-now button {
            background-color: #5bc0de;
            color: #fff;
            border: none;
            padding: 10px 20px;
            font-size: 24px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

            .book-now button:hover {
                background-color: #31b0d5;
            }

        .booking-info {
            background-color: #fff;
            color: #000;
            padding: 20px;
            border-radius: 10px;
            width: 100%;
        }


        .time {
            font-weight: bold;
            flex: 0 0 80px;
        }

        .title {
            flex-grow: 1;
            margin-left: 10px;
        }

        .location {
            font-style: italic;
            flex: 0 0 150px;
            text-align: right;
        }

        .content {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            height: 100%;
            overflow-y: auto;
        }

        #addBookingContent, #myBookingsContent {
            display: flex;
        }




        .modal {
            display: none;
            position: fixed;
            z-index: 50;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.4);
            padding-top: 20px; 
            padding-bottom: 20px; 
        }

        .modal-content {
            background-color: #185D62;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #185D62;
            width: 80%;
            max-width: 700px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
            font-family: 'Poppins', sans-serif;
            color: white;
            position: relative;
        }

            .modal-content h1 {
                text-align: center;
                color: #ecf0f1;
                margin-bottom: 10px;
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: 1px;
            }

            .modal-content form {
                background: #fff;
                padding: 15px;
                border-radius: 8px;
                display: flex;
                flex-wrap: wrap;
                gap: 8px; 
            }

            .modal-content label {
                display: block;
                margin-bottom: 2px; 
                font-weight: bold;
                font-size: 0.9em;
                color: #000;
            }

            .modal-content input,
            .modal-content select {
                width: 100%;
                padding: 6px; 
                margin-bottom: 6px;
                border: 1px solid #ccc;
                border-radius: 4px;
                box-sizing: border-box;
                background-color: #ecf0f1;
                color: #2c3e50;
            }

        .form-group {
            flex: 1 1 calc(50% - 8px);
            min-width: 200px;
        }

        .full-width {
            flex: 1 1 100%;
        }

        .form-group-half {
            flex: 1 1 calc(50% - 8px); 
            min-width: calc(50% - 8px);
            margin-right: 10px;
        }

        .modal-content button {
            background-color: #5bc0de;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 600;
            transition: background-color 0.3s ease;
            margin-right: 10px;
            margin-top: 15px;
        }

            .modal-content button:hover {
                background-color: #31b0d5;
            }

            .modal-content button[type="submit"] {
                background-color: #1893a2;
            }

                .modal-content button[type="submit"]:hover {
                    background-color: #0d353f;
                }

            .modal-content button[type="reset"] {
                background-color: #35d3db;
            }

                .modal-content button[type="reset"]:hover {
                    background-color: #19b6c1;
                }

        .button-group {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .close {
            color: #fff;
            position: absolute;
            top: 10px;
            right: 15px;
            font-size: 24px; 
            font-weight: bold;
        }

            .close:hover,
            .close:focus {
                color: #ecf0f1;
                text-decoration: none;
                cursor: pointer;
            }

        #recurrenceOptions {
            margin-top: 6px;
        }

        .form-group:last-child {
            margin-bottom: 0;
        }

        .table-container {
            overflow-x: auto;
            overflow-y: auto;
            max-height: calc(76vh - 10rem); 
        }

        .error-message {
            color: red;
            font-size: 0.9em;
            margin-top: 5px;
            display: none;
        }

        /* for booking details modal */
        #bookingDetailsModal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.4);
        }

            #bookingDetailsModal .modal-content {
                background-color: #185D62;
                margin: 10% auto;
                padding: 20px;
                border: 1px solid #185D62;
                width: 80%;
                max-width: 600px;
                border-radius: 10px;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
                font-family: 'Poppins', sans-serif;
                color: white;
                position: relative;
            }

                #bookingDetailsModal .modal-content h3 {
                    text-align: center;
                    color: #ecf0f1;
                    margin-bottom: 10px;
                    font-size: 2em;
                    font-weight: 600;
                    text-transform: uppercase;
                    letter-spacing: 1px;
                }

                #bookingDetailsModal .modal-content .close {
                    color: #fff;
                    position: absolute;
                    top: 10px;
                    right: 15px;
                    font-size: 24px;
                    font-weight: bold;
                    cursor: pointer;
                }

                    #bookingDetailsModal .modal-content .close:hover,
                    #bookingDetailsModal .modal-content .close:focus {
                        color: #ecf0f1;
                        text-decoration: none;
                    }

        .white-container {
            background-color: white;
            color: #185D62;
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
        }

            .white-container p {
                margin-bottom: 10px;
                line-height: 1.6;
                font-size: 1em;
            }

            .white-container strong {
                color: #185D62;
                font-weight: 600;
            }

        .dropdown-container {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }

            .dropdown-content a {
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
                cursor: pointer;
            }

                .dropdown-content a:hover {
                    background-color: #f1f1f1;
                }

        .show {
            display: block;
        }

        .show {
            display: block;
        }

        #roleDropdown {
            color: black;
        }

        #bookingsDropdown {
            color: black;
        }

        #roomDropdown {
            color: black;
        }


        .day-selector {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .day {
            width: 30px;
            height: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #ccc;
            border-radius: 5px;
            cursor: pointer;
        }

            .day.selected {
                background-color: #185D62;
                border: 2px solid #000;
            }

        #cancelModal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.6);
        }

            #cancelModal .modal-content {
                background-color: #185D62;
                margin: 15% auto;
                padding: 30px;
                border: none;
                width: 90%;
                max-width: 500px;
                border-radius: 15px;
                box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
                font-family: 'Poppins', sans-serif;
                color: white;
                position: relative;
                text-align: center;
            }

                #cancelModal .modal-content h1 {
                    font-size: 2rem;
                    color: #ecf0f1;
                    margin-bottom: 20px;
                    font-weight: 600;
                    text-transform: uppercase;
                    letter-spacing: 1px;
                }

                #cancelModal .modal-content p {
                    font-size: 1.1rem;
                    margin-bottom: 25px;
                    line-height: 1.5;
                }

            #cancelModal .close {
                position: absolute;
                top: 10px;
                right: 15px;
                color: #ecf0f1;
                font-size: 28px;
                font-weight: bold;
                cursor: pointer;
                transition: color 0.3s ease;
            }

                #cancelModal .close:hover {
                    color: #bdc3c7;
                }

            #cancelModal .button-container {
                display: flex;
                justify-content: center;
                gap: 15px;
            }

            #cancelModal .btn {
                padding: 12px 24px;
                font-size: 1rem;
                font-weight: 600;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                transition: background-color 0.3s ease, transform 0.2s ease;
                margin: 0 10px;
            }

            #cancelModal .btn-danger {
                background-color: #813d33;
                color: white;
            }

            #cancelModal .btn-secondary {
                background-color: #9a9999;
                color: white;
            }

            #cancelModal .btn:hover {
                transform: translateY(-2px);
            }

            #cancelModal .btn-danger:hover {
                background-color: #ff2348;
            }

            #cancelModal .btn-secondary:hover {
                background-color: #6c7a7d;
            }

        .search-container {
            position: relative;
            display: flex;
            align-items: center; /* Align items vertically in the center */
            width: 100%; /* Make the container take full width of its parent */
        }

        .search-input {
            flex: 1; /* Allow the input to grow and fill available space */
            padding-right: 2.5rem; /* Make room for the clear button */
            border: 1px solid #d1d5db; /* Border color */
            border-radius: 0.375rem; /* Rounded corners */
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05); /* Shadow effect */
            outline: none; /* Remove default focus outline */
            transition: border-color 0.2s, box-shadow 0.2s; /* Smooth transition on focus */
        }

            .search-input:focus {
                border-color: #3b82f6; /* Focus border color */
                box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2); /* Focus shadow */
            }

        .clear-button {
            position: absolute;
            right: 0.99rem; /* Position the button inside the input */
            top: 50%;
            transform: translateY(-50%); /* Center the button vertically */
            background: transparent;
            border: none;
            cursor: pointer;
            color: darkred; /* Button color */
            font-size: 1rem;
        }

            .clear-button:hover {
                color: red; /* Darker color on hover */
            }
    </style>
}