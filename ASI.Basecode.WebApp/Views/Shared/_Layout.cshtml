<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="logo-light-vertical.png" type="image/png">
    <title>@ViewData["Title"]</title>
    @*<link rel="icon" type="image/png" href="C:\Users\Angel Rose Comajes\Downloads\bago\ASI.Basecode.WebApp\wwwroot\img\logo-light-vertical.png">*@
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/main.css" />
    <link rel="stylesheet" href="~/css/style.css" />
    <link rel="stylesheet" href="~/css/admin.css" id="theme-style" />
    <link rel="stylesheet" href="~/css/admindark.css" id="theme-style" />

    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css">
    @RenderSection("Styles", required: false)
    

</head>
<body class="@((Model != null && Model.DarkMode) ? "light" : "dark")">
    @Html.Partial("_Header")
    @{
        var userRole = Context.Session.GetString("UserRole");
        var isAdmin = userRole == "0"; // Assuming 0 is the role ID for Admin
        var isLoginOrRegister = ViewContext.RouteData.Values["Controller"].ToString() == "Account" &&
        (ViewContext.RouteData.Values["Action"].ToString() == "Login" ||
        ViewContext.RouteData.Values["Action"].ToString() == "Register");
    }

    @if (!isLoginOrRegister)
    {
        if (isAdmin)
        {
            @Html.Partial("_AdminSidebar")
        }
        else
        {
            @Html.Partial("_UserSidebar")
        }
    }

    <div class="container" style="@(isLoginOrRegister ? "centered;" : "margin-left: 250px;")">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <!--<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.3/jquery.validate.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/3.2.12/jquery.validate.unobtrusive.js"></script>  -->
    <script src="https://kit.fontawesome.com/7a7c645416.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>
    <script>
        // Configure Toastr options
        toastr.options = {
            positionClass: "toast-top-center", // Set the notification position to center
            timeOut: 3000,
            closeButton: true,
            progressBar: true
        };
        /*
        document.addEventListener('DOMContentLoaded', function () {
            fetchPreferences();
        });

        function fetchPreferences() {
            const userId = '@HttpContextAccessor.HttpContext.Session.GetString("UserId")';
            if (!userId) {
                console.error('User ID not found in session storage.');
                return;
            }

            fetch(`/api/Preference/myPreferences?userId=${userId}`)
                .then(response => response.json())
                .then(data => {
                    if (data) {
                        document.querySelector('input[name="DarkMode"]').checked = data.darkMode;
                        document.querySelector('select[name="TimeFormat"]').value = data.timeFormat.toString();
                        document.querySelector('select[name="DefaultBookingDuration"]').value = data.defaultBookingDuration.toString();

                        const preferences = {
                            darkMode: data.darkMode,
                            timeFormat: data.timeFormat,
                            defaultBookingDuration: data.defaultBookingDuration
                        };
                        sessionStorage.setItem('userPreferences', JSON.stringify(preferences));

                        applyTheme(data.darkMode);
                    } else {
                        console.log('No preferences found. Setting default values or handling empty state.');
                    }
                })
                .catch(error => console.error('Error fetching preferences:', error));
        }

        const darkModeToggle = document.querySelector('#dark-change input');
        darkModeToggle.addEventListener('change', function () {
            const theme = this.checked ? 'dark' : 'light';
            document.body.className = theme;
            const preferences = JSON.parse(sessionStorage.getItem('userPreferences')) || {};
            preferences.darkMode = this.checked;
            sessionStorage.setItem('userPreferences', JSON.stringify(preferences));
            applyTheme(this.checked);
        });

        function applyTheme(isDarkMode) {
            const body = document.body;
            const darkStyle = document.getElementById('theme-style-dark');
            const lightStyle = document.getElementById('theme-style-light');

            if (isDarkMode) {
                body.classList.add('dark');
                body.classList.remove('light');
                darkStyle.disabled = false;
                lightStyle.disabled = true;
            } else {
                body.classList.add('light');
                body.classList.remove('dark');
                darkStyle.disabled = true;
                lightStyle.disabled = false;
            }
        }

        document.getElementById('preferencesForm').addEventListener('submit', function (event) {
            event.preventDefault();
            savePreferences();
        });

        function savePreferences() {
            const preferences = {
                Id: parseInt(sessionStorage.getItem('UserId')),
                darkMode: document.querySelector('input[name="DarkMode"]').checked,
                timeFormat: parseInt(document.querySelector('select[name="TimeFormat"]').value),
                defaultBookingDuration: parseInt(document.querySelector('select[name="DefaultBookingDuration"]').value)
            };

            fetch('/api/Preference/save', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(preferences)
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        console.log('Preferences saved successfully.');
                        applyTheme(preferences.darkMode);
                        displaySuccessMessage();
                    } else {
                        console.error('Failed to save preferences:', data.message);
                    }
                })
                .catch(error => console.error('Error saving preferences:', error));
        }

        document.addEventListener("DOMContentLoaded", function () {
            function adjustFontSize() {
                const zoomLevel = Math.round(window.devicePixelRatio * 100);
                const rootFontSize = 16 * window.devicePixelRatio;
                document.documentElement.style.fontSize = `${rootFontSize}px`;

                const weight = zoomLevel > 100 ? "bold" : "normal";
                document.body.style.fontWeight = weight;

                const sidebar = document.querySelector(".sidebar");
                if (sidebar) {
                    sidebar.style.width = "265px";
                    sidebar.style.fontSize = "initial";
                }
            }

            window.addEventListener("resize", adjustFontSize);
            adjustFontSize();
        });*/

        document.addEventListener('DOMContentLoaded', function () {
            fetchPreferences();
            const userId = '@HttpContextAccessor.HttpContext.Session.GetString("UserId")';
            if (!userId) {
                console.error('User ID not found in session storage.');
                return;
            }
        });

        function fetchPreferences() {
            fetch('/api/Preference/myPreferences')
                .then(response => {
                    if (!response.ok) {
                        throw new Error('Network response was not ok');
                    }
                    return response.json();
                })
                .then(data => {
                    if (data) {
                        applyTheme(data.darkMode);
                    } else {
                        console.error('No preferences found. Setting default values or handling empty state.');
                    }
                })
                .catch(error => console.error('Error fetching preferences:', error));
        }

        const darkModeToggle = document.querySelector('#dark-change input');
        darkModeToggle.addEventListener('change', function () {
            const theme = this.checked ? 'dark' : 'light';
            document.body.className = theme;
            const preferences = JSON.parse(sessionStorage.getItem('userPreferences')) || {};
            preferences.darkMode = this.checked;
            sessionStorage.setItem('userPreferences', JSON.stringify(preferences));
            applyTheme(this.checked);
        });

        
        document.getElementById('preferencesForm').addEventListener('submit', function (event) {
            event.preventDefault();
            savePreferences();
        });

        function savePreferences() {
            const preferences = {
                Id: parseInt(sessionStorage.getItem('UserId')),
                darkMode: document.querySelector('input[name="DarkMode"]').checked,
                timeFormat: parseInt(document.querySelector('select[name="TimeFormat"]').value),
                defaultBookingDuration: parseInt(document.querySelector('select[name="DefaultBookingDuration"]').value)
            };

            fetch('/api/Preference/save', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(preferences)
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        console.log('Preferences saved successfully.');
                        applyTheme(preferences.darkMode);
                        displaySuccessMessage();
                    } else {
                        console.error('Failed to save preferences:', data.message);
                    }
                })
                .catch(error => console.error('Error saving preferences:', error));
        }

        function applyTheme(isDarkMode) {
            const body = document.body;
            if (isDarkMode) {
                body.classList.add('light');
                body.classList.remove('dark');
                loadTheme('adminlight.css');
            } else {
                body.classList.add('dark');
                body.classList.remove('light');
                loadTheme('admin.css');
            }
        }

        function loadTheme(themeName) {
            const themeStyle = document.getElementById('theme-style');
            if (themeStyle) {
                themeStyle.setAttribute('href', `/css/${themeName}`);
            } else {
                console.error('Theme style link element not found');
            }
        }
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>