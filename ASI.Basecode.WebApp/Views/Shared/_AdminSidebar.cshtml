@{
    var userRole = Context.Session.GetString("UserRole");
    var isAdmin = userRole == "0";
}

<link rel="stylesheet" href="~/css/admin.css" />
<div class="sidebar">
    <div class="logo">
        <img id="logo-dark" src="~/img/logo-dark-horizontal.png" alt="Dark Mode Logo" style="display: block;">
        <img id="logo-light" src="~/img/logo-light-horizontal.png" alt="Light Mode Logo" style="display: none;">
    </div>
    <ul class="sidebar-menu">
        <li class="@(ViewContext.RouteData.Values["Action"].ToString() == "Index" ? "active" : "")">
            <a asp-controller="Admin" asp-action="Index">
                <i class="fas fa-home"></i>
                <span>Home</span>
            </a>
        </li>
        <li class="@(ViewContext.RouteData.Values["Action"].ToString() == "Analytics" ? "active" : "")">
            <a asp-controller="Admin" asp-action="Analytics">
                <i class="fas fa-chart-bar"></i>
                <span>Analytics</span>
            </a>
        </li>
        
        <li class="@(ViewContext.RouteData.Values["Action"].ToString() == "AdminSettings" ? "active" : "")">
            <a asp-controller="Admin" asp-action="AdminSettings">
                <i class="fas fa-cog"></i>
                <span>Settings</span>
            </a>
        </li>
    </ul>
    <div class="user-details">
        <span class="side-user-name ml-2 overflow-hidden">Hi! @HttpContextAccessor.HttpContext.Session.GetString("UserName")</span>
    </div>
    <div class="signout">
        <a asp-action="SignOutAdmin" asp-controller="Account">
            <i class="fas fa-sign-out-alt"></i>
            <span>Logout</span>
        </a>
    </div>
</div>